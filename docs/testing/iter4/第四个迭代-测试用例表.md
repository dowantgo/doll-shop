# 第四个迭代测试用例表

## 范围

- 首页热榜与销量排行缓存治理
- `price-preview`、`refund-create`、`logistics-query` 性能复测
- Service 分层后接口字段与状态码回归
- Redis 命中率统计与失效入口验证
- Iter3 交易闭环全量回归

## 用例清单

| 编号 | 模块 | 测试点 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| TC-I4-001 | Feed 缓存 | `top-sales` 接口返回正常且排序正确 | 返回稳定，字段完整，排序正确 | 待测 |
| TC-I4-002 | Feed 缓存 | `hot-feed` 接口返回正常且轮播数据可用 | 返回稳定，字段完整，可用于前端轮播 | 待测 |
| TC-I4-003 | Feed 缓存 | 商品变更后 feed 缓存失效并重建 | 版本号更新，新数据可见 | 待测 |
| TC-I4-004 | 缓存命中率 | `top-sales` 命中率可统计 | 能拿到 hit/miss 统计值 | 待测 |
| TC-I4-005 | 缓存命中率 | `hot-feed` 命中率可统计 | 能拿到 hit/miss 统计值 | 待测 |
| TC-I4-006 | 价格试算 | `price-preview` 接口性能无回退 | P95 低于 Iter3 基线 | 待测 |
| TC-I4-007 | 退款 | `refund-create` 性能改善且功能正确 | 创建成功率 100%，P95 优于 Iter3 | 待测 |
| TC-I4-008 | 物流 | `logistics-query` 性能稳定且功能正确 | 查询稳定，P95 在目标范围内 | 待测 |
| TC-I4-009 | 架构分层 | service 分层后接口响应结构不漂移 | 前端联调无字段兼容问题 | 待测 |
| TC-I4-010 | 回归 | Iter3 优惠券、退款、物流主链路全绿 | 功能与状态机正确 | 待测 |

## 执行建议

1. 先做功能回归，再做同口径压测。
2. 性能指标必须和 Iter3 保持同一压测参数，才能做有效对比。
3. `price-preview` 属于本轮重点观察对象，需要单独看回退与修复前后差异。
