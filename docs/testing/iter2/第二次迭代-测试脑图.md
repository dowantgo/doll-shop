# 第二次迭代测试脑图（Markdown版）

## 0. 目标与边界
- 目标：验证二迭代增量功能已可回归，迭代1核心链路不回归。
- 口径：严格区分 `已落地`、`已实现（API）`、`预埋（未上线）`、`规划中`。
- 交付物：功能测试、接口测试、性能基线、回归清单。

## 1. 二迭代增量功能
### 1.1 评价系统
- 已购用户可评价
- 非已购用户拦截
- 同订单项一评
- 敏感词过滤与反馈
- 评价回复能力
- 我的评价列表

### 1.2 销量排行
- 排行接口可用
- 字段完整性校验
- 首页展示联动
- 空态降级

### 1.3 热榜轮播
- 热榜接口可用
- 自动轮播
- 手动切换
- 无数据降级

## 2. P0 回归清单
### 2.1 秒杀状态机
- 活动状态字段是否统一
- 前后台状态展示是否一致
- 非法状态流转是否被拒绝

### 2.2 秒杀超时回收
- 预占 TTL 到期自动释放
- 库存是否回补
- 预占状态是否变更

### 2.3 支付补偿
- 支付回调成功可入账
- 回调失败能否补偿修复
- 重试日志是否可追踪

### 2.4 敏感词反馈
- 敏感词命中是否返回提示
- 清洗后内容是否可回显
- 前端提示是否可引导修改

## 3. 接口验收清单
- `GET /api/products/products/top-sales/`
- `GET /api/products/products/hot-feed/`
- `GET /api/reviews/product/{id}/`
- `POST /api/reviews/product/{id}/`
- `GET /api/reviews/my/`
- `PATCH /api/admin/reviews/{id}/audit/`
- `GET /api/admin/reviews/`
- `GET /api/seckill/activities/`
- `POST /api/seckill/pre-reserve/`

## 4. 迭代1核心回归
- 注册 / 登录 / 鉴权
- 商品浏览 / 搜索 / 筛选
- 加入购物车 / 修改数量 / 删除
- 下单 / 取消待支付 / 订单列表
- 支付创建 / 回调状态一致性

## 5. 性能与稳定性
- top-sales 的 P95
- hot-feed 的 P95
- 榜单/热榜缓存命中率
- 首页首屏耗时
- 秒杀预占限流触发率

## 6. 出口条件
- 所有 P0 回归点通过
- 接口验收清单通过
- 迭代1核心回归通过
- 性能基线可记录、可对比
